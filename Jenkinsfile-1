pipeline {
    agent any
    stages {
        stage('Test SonarQube Connection') {
            steps {
                script {
                    def sonarUrl = env.sq1
                    // El comando 'curl' realiza una solicitud HTTP GET a SonarQube
                    sh "curl -I \${sonarUrl}"
                }
            }
        }
    }
}
